import { property } from "@/lib/property";
export function Contact(){const r=property.realtor;return <section id="contact" className="section contact"><div className="container contact-grid"><div><p className="eyebrow">Private showings</p><h2>Come experience the estate.</h2><p className="lead">Photos tell part of the story. The privacy, scale, trails and view are best understood in person.</p><div className="contact-actions"><a className="button button-dark" href={`mailto:${r.email}?subject=Enumclaw Forest Estate Showing`}>Email Todd</a><a className="button button-plain" href={`tel:+1${r.phone.replaceAll("-","")}`}>{r.phone}</a></div></div><aside className="agent-card"><img src={r.logoUrl} alt="KW Mountains to Sound Realty"/><h3>{r.name}</h3><p>{r.company}</p><a href={`mailto:${r.email}`}>{r.email}</a><a href={`tel:+1${r.phone.replaceAll("-","")}`}>{r.phone}</a><small>Listing information is believed reliable but is not guaranteed. Buyer to verify all information.</small></aside></div></section>}
